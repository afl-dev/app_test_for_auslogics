name: app_test_for_auslogics
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.1
  bloc: ^4.0.0
  flutter_bloc: ^4.0.1
  moor_flutter: ^3.0.0
  moor: # use the latest version
  moor_ffi: # use the latest version
  path_provider:
  path:
  video_player: ^0.10.11+1
  intl: ^0.16.1
  dart_utils: ^1.0.2
  #^0.9.10
  chewie:
    path: ./flutterLib/chewie_master_override

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^0.1.9
  flutter_launcher_icons: ^0.7.5
  build_runner: ^1.10.0
  moor_generator:


flutter_native_splash:
  image: assets/sun_PNG13449.png
  color: "ffffff"

flutter_icons:
  image_path: "assets/sun_PNG13449.png"
  android: true
  ios: true

flutter:

  uses-material-design: true
  assets:
    - assets/
